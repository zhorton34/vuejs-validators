'use strict';

module.exports = function within(_ref) {
  var message = _ref.message,
      attribute = _ref.attribute,
      rule = _ref.rule,
      parameters = _ref.parameters;
  return parameters.includes(value);
};